import Request from '../request'

// 首页数据请求api
const currBaseUrl = '/portal/'

// export const getPortalData = () => {
//   Request.request(Request.get(), currBaseUrl).then((data) => {
//     if(data.data){
//     }
//   })
// }
