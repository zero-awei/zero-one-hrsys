#include "stdafx.h"
#include "AddAssignInfoService.h"
#include "dao/sample/SampleDAO.h"
uint64_t AddAssignInfoService::saveData(const AddAssignInfoDTO::Wrapper& dto)
{
	// 组装DO数据
	//AssignInfoDO data;
	// 	data.setName(dto->name.getValue(""));
	// 	data.setSex(dto->sex.getValue(""));
	// 	data.setAge(dto->age.getValue(1));
	//ZO_STAR_DOMAIN_DTO_TO_DO(data, id, Id, assign, Assign, type_, Type_, depart, Depart, job, Job, post, Post, start_time, Start_time, end_time, End_time);
		// 执行数据添加
		//SampleDAO dao;
	return NULL;
}